require('../setup');
import * as libjs from '../../platforms/linux-x86_64';
import {expect} from 'chai';
/*
describe('Linux x86_64 > fcntl', () => {
    it('is a function', () => {
        expect(typeof libjs.fcntl).to.eql('function');
    });

    it('can execute basic fcntl() operation', () => {
        const res = libjs.fcntl(1, 1, 0);
        expect(res).eql(1);
    });
});
*/
